"""
1. RECURSIVE CTE(COMMON TABLE EXPRESSION)
WITH RECURSIVE HOUR_24 AS (
SELECT
    0 AS HOUR
    
UNION ALL
    
SELECT
    HOUR + 1 AS HOUR
FROM
    HOUR_24
WHERE
    HOUR < 23
)

SELECT
    H.HOUR,
    COUNT(I.ANIMAL_ID) AS COUNT
FROM
    HOUR_24 H
LEFT JOIN
    ANIMAL_OUTS I
    ON
    HOUR(I.DATETIME) = H.HOUR
GROUP BY
    H.HOUR
ORDER BY
    H.HOUR
"""
